# ===================================
# ScriptAI MVP - 生产环境配置模板
# ===================================
#
# 重要提示:
# 1. 不要在本地创建 .env.production 文件
# 2. 在Vercel Dashboard中配置这些环境变量
# 3. 此文件仅作为参考
#

# -----------------------------------
# 数据库配置 (Supabase)
# -----------------------------------

# 主数据库连接 (使用Connection Pooler - 端口6543)
# 必须包含 pgbouncer=true 和 connection_limit=1
DATABASE_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true&connection_limit=1"

# 直连URL (数据库迁移使用 - 端口5432)
DIRECT_URL="postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres"

# -----------------------------------
# DeepSeek AI配置
# -----------------------------------

# DeepSeek API密钥 (生产环境使用独立密钥)
DEEPSEEK_API_KEY=sk-production-key-here

# DeepSeek API端点
DEEPSEEK_API_URL=https://api.deepseek.com

# -----------------------------------
# 生产模式配置
# -----------------------------------

# 运行环境
NODE_ENV=production

# 不设置 DISABLE_RATE_LIMIT (默认启用速率限制)
